(function (window, document) {
  'use strict';
  var dictionary = {id:{}, en:{}};
  var entries = {
    'mapPanels.map':['Peta & layer','Map & layers'],
    'mapPanels.info':['Informasi astronomi','Astronomical information'],
    'mapPanels.navigation':['Panel peta','Map panels'],
    'mapPresentation.title':['Tampilan peta untuk reviewer','Map appearance for reviewer'],
    'mapPresentation.layer':['Lapisan','Layer'],
    'mapPresentation.visible':['Terlihat','Visible'],
    'mapPresentation.opacity':['Opasitas','Opacity'],
    'mapPresentation.order':['Urutan lapisan','Layer order'],
    'mapPresentation.token':['Token admin untuk simpan bersama','Admin token for shared save'],
    'mapPresentation.save':['Simpan bersama ke SQL','Save shared configuration to SQL'],
    'mapPresentation.reload':['Muat konfigurasi','Load configuration'],
    'mapPresentation.local':['Tersimpan di perangkat ini; reviewer pada perangkat ini memakai pilihan yang sama.','Saved on this device; its reviewer uses the same choices.'],
    'mapPresentation.loaded':['Konfigurasi peta dimuat.','Map configuration loaded.'],
    'mapPresentation.saving':['Menyimpan konfigurasi bersama…','Saving shared configuration…'],
    'mapPresentation.saved':['Konfigurasi bersama tersimpan di SQL.','Shared configuration saved to SQL.'],
    'mapPresentation.authorization':['Masukkan token admin yang valid untuk menyimpan konfigurasi bersama. Pilihan lokal tetap tersimpan.','Enter a valid admin token to save shared configuration. Local choices are preserved.'],
    'mapPresentation.unavailable':['Konfigurasi bersama belum tersedia. Pilihan lokal tetap dapat dipakai.','Shared configuration is unavailable. Local choices remain usable.'],
    'mapPresentation.storageFailed':['Penyimpanan lokal gagal. Simpan ke SQL untuk mempertahankan pilihan.','Local storage failed. Save to SQL to retain your choices.'],
    'mapLayer.graticule':['Garis lintang/bujur','Graticule'],
    'mapLayer.atmosphere':['Atmosfer','Atmosphere'],
    'mapLayer.prayer':['Zona salat','Prayer zones'],
    'mapLayer.hilal':['Hilal','Crescent visibility'],
    'mapLayer.naturalEarth':['Perairan Natural Earth','Natural Earth water'],
    'mapLayer.adm':['Batas administratif','Administrative boundaries'],
    'mapLayer.trajectory':['Trajektori','Trajectories'],
    'mapLayer.sunTrajectory':['Trajektori matahari','Sun trajectory'],
    'mapLayer.moonTrajectory':['Trajektori bulan','Moon trajectory'],
    'mapLayer.direction':['Arah kompas','Compass directions'],
    'mapLayer.bodies':['Objek astronomi','Astronomical objects'],
    'mapLayer.eclipse':['Gerhana','Eclipses'],
    'mapLayer.context':['Bangunan, penyedia, dan Kaabah','Buildings, provider, and Kaabah'],
    'mapLayer.onlineOverlay':['Overlay peta online','Online map overlay'],
    'mapLayer.weatherOverlay':['Overlay cuaca','Weather overlay'],
    'selector.question':['Apa data yang ingin Anda tampilkan pada layer peta ini?','What data would you like to display on this map layer?'],
    'selector.adm0':['Negara','Country'],
    'selector.adm1':['Provinsi','Province/State'],
    'selector.adm2':['Kabupaten/Kota','Regency/City'],
    'selector.chooseadm0':['Pilih negara','Select country'],
    'selector.chooseadm1':['Pilih provinsi','Select province/state'],
    'selector.chooseadm2':['Pilih kabupaten/kota','Select regency/city'],
    'selector.loading':['Memuat pilihan bangunan…','Loading building choices…'],
    'selector.unavailable':['Pilihan bangunan belum tersedia. Periksa koneksi database.','Building choices are unavailable. Check the database connection.'],
    'selector.empty':['Tidak ada data bangunan yang sesuai dengan filter.','No buildings match the selected filters.'],
    'selector.previous':['Sebelumnya','Previous'],
    'selector.next':['Berikutnya','Next'],
    'selector.page':['Halaman {page} dari {pages}','Page {page} of {pages}'],
    'waterScaleLabel':['Skala data perairan','Water data scale'],
    'building.identifier':['Identifier bangunan','Building identifier'],
    'building.identifierPending':['Dibuat server setelah disimpan','Generated by the server after saving'],
    'building.searchIdentifier':['Cari identifier bangunan','Search building identifier'],
    'actions.copyIdentifier':['Salin identifier','Copy identifier'],
    'messages.identifierCopied':['Identifier disalin.','Identifier copied.'],
    'messages.copyFailed':['Tidak dapat menyalin. Pilih dan salin identifier secara manual.','Unable to copy. Select and copy the identifier manually.'],
    'messages.buildingNotFound':['Bangunan tidak ditemukan.','Building not found.'],
    'messages.invalidIdentifier':['Identifier bangunan tidak valid.','Invalid building identifier.'],
    'messages.buildingUnavailable':['Data bangunan belum dapat dimuat. Periksa koneksi lalu coba lagi.','Building data is unavailable. Check the connection and retry.'],
    'messages.buildingCoordinatesMissing':['Bangunan belum memiliki koordinat.','Building has no coordinates.'],
    'nav.astronomicalReviewer':['Tinjauan astronomi','Astronomical reviewer'],
    'learning.hijaiyahIntroduction':['Kenali nama serta bentuk tunggal, awal, tengah, dan akhir. Bacaan awal Iqro dimulai dari Alif berfathah (a pendek), lalu Ba, Ta, Tsa.','Learn letter names and isolated, initial, medial and final forms. Iqro reading starts with Alif with fathah (short a), followed by Ba, Ta and Tha.']
  };
  Object.keys(entries).forEach(function (key) { dictionary.id[key]=entries[key][0]; dictionary.en[key]=entries[key][1]; });
  window.PrayerI18n.mergeDictionary(dictionary);
  // Catalog only contains UI strings reviewed as UI. Arabic/source/user content
  // is never machine-translated, and input/textarea values are never rewritten.
  var catalog = window.MpmUiCatalog || [];
  var lookup = Object.create(null);
  function keyFor(text) {
    var hash=2166136261;
    for (var i=0;i<text.length;i++) hash=Math.imul(hash ^ text.charCodeAt(i),16777619);
    return 'ui.'+(hash>>>0).toString(16);
  }
  function register(pairs) {
    var additions={id:{},en:{}};
    pairs.forEach(function (pair) {
      var key=keyFor(pair[0]); additions.id[key]=pair[0]; additions.en[key]=pair[1];
      lookup[pair[0]]=key; lookup[pair[1]]=key;
    });
    window.PrayerI18n.mergeDictionary(additions);
  }
  register(catalog);
  function uiText(value) {
    var text=String(value == null ? '' : value), key=lookup[text.trim()];
    return key ? text.replace(/\S[\s\S]*\S|\S/,window.PrayerI18n.t(key)) : text;
  }
  window.PrayerI18n.uiText=uiText;
  ['alert','confirm'].forEach(function (name) {
    var native=window[name];
    window[name]=function (message) {return native.call(window,uiText(message));};
  });
  var protectedSelector='[lang="ar"], [translate="no"], [data-no-i18n], [contenteditable], textarea, pre, code, script, style, .it-arabic, .it-translation, .it-tafsir-text, .it-hadith-text, .worship-evidence-text, .overlay-content';
  var nodes=new WeakMap();
  function translateNode(node, controlsOnly) {
    var parent=node.parentElement;
    if (!parent || parent.closest(protectedSelector)) return;
    if (controlsOnly && !parent.closest('button, label, summary, th, .it-help, .it-empty, .empty-state, .status-message, [role="status"], [role="alert"], [data-ui-text]')) return;
    var previous=nodes.get(node), raw=node.nodeValue;
    var key=previous && raw===previous.rendered ? previous.key : lookup[raw.trim()];
    if (!key) return;
    var rendered=raw.replace(/\S[\s\S]*\S|\S/,window.PrayerI18n.t(key));
    if (rendered!==raw) node.nodeValue=rendered;
    nodes.set(node,{key:key,rendered:rendered});
  }
  function apply(root) {
    if (root.nodeType===3) { translateNode(root,true); return; }
    if (!root.querySelectorAll) return;
    window.PrayerI18n.applyTranslations(root);
    root.querySelectorAll('[data-ui-key]').forEach(function (element) {
      Array.from(element.childNodes).filter(function (n) {return n.nodeType===3;}).forEach(function (node) {translateNode(node,false);});
    });
    root.querySelectorAll('button,label,summary,th,.it-help,.it-empty,.empty-state,.status-message,[role="status"],[role="alert"],[data-ui-text]').forEach(function (element) {
      var walker=document.createTreeWalker(element,NodeFilter.SHOW_TEXT);var node;
      while ((node=walker.nextNode())) translateNode(node,true);
    });
  }
  // Call only on an authored UI skeleton, before source or user data is added.
  function markStatic(root) {
    var walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT), texts=[], node;
    while ((node=walker.nextNode())) texts.push(node);
    texts.forEach(function(text) {
      var parent=text.parentElement,key=lookup[text.nodeValue.trim()];
      if(!key||!parent||parent.closest(protectedSelector)||parent.closest('[data-i18n]'))return;
      if(parent.tagName==='OPTION') {
        if(!parent.hasAttribute('value'))parent.value=parent.textContent;
        parent.dataset.i18n=key;
      } else {
        var span=document.createElement('span');span.dataset.i18n=key;span.textContent=text.nodeValue;
        text.replaceWith(span);
      }
    });
    apply(root);
  }
  window.MpmUiTranslations={register:register,keyFor:keyFor,apply:apply,markStatic:markStatic};
  document.addEventListener('DOMContentLoaded',function () {
    apply(document);
    var scheduled=false, pending=new Set();
    new MutationObserver(function (mutations) {
      mutations.forEach(function (m) {pending.add(m.target.nodeType===3 ? m.target : m.target);});
      if (scheduled) return; scheduled=true;
      requestAnimationFrame(function () {scheduled=false;var roots=Array.from(pending);pending.clear();roots.forEach(function (root) {apply(root);});});
    }).observe(document.body,{childList:true,subtree:true,characterData:true});
  });
  window.addEventListener('mpm:language-changed',function () {apply(document);});
})(window,document);
